# TEST-1775951346 — Add formatCurrency utility function

Implement a `formatCurrency` utility function that formats numbers as currency strings with support for USD (default) and other currencies like EUR.

## Wave 1

- [x] 1. Add formatCurrency function to src/index.ts
  - Implement `formatCurrency(amount: number, currency?: string): string`
  - Default to USD ('$') when no currency specified
  - Format with 2 decimal places and thousands separators (e.g., 1234.5 → '$1,234.50')
  - Handle negative numbers correctly (e.g., -99.99 → '-$99.99')
  - Support EUR currency symbol ('€') when 'EUR' is passed

## Wave 2

- [ ] 1. Add unit tests for formatCurrency in src/index.test.ts
  - Test default USD formatting: formatCurrency(1234.5) → '$1,234.50'
  - Test large number formatting: formatCurrency(1000000) → '$1,000,000.00'
  - Test negative number formatting: formatCurrency(-99.99) → '-$99.99'
  - Test EUR currency: formatCurrency(100, 'EUR') → '€100.00'

## Wave 3

- [ ] 1. Run TypeScript compiler (npm run lint) to verify no type errors
- [ ] 2. Run vitest tests (npm run test) to verify all tests pass
