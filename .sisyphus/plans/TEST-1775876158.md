# TEST-1775876158 — Add formatCurrency utility function

Implement a `formatCurrency(amount: number, currency?: string): string` utility function that formats numbers as currency strings with proper locale-aware thousand separators and decimal places.

## Wave 1

- [x] 1. Implement formatCurrency function signature and USD default formatting
- [x] 2. Add basic formatCurrency tests for positive USD amounts (1234.5 → '$1,234.50', 1000000 → '$1,000,000.00')

## Wave 2

- [x] 1. Add formatCurrency support for negative amounts (-99.99 → '-$99.99')
- [x] 2. Add formatCurrency support for other currency codes (EUR → '€', GBP → '£')
- [x] 3. Run TypeScript typecheck (tsc --noEmit) to verify clean compilation
- [x] 4. Run vitest to verify all tests pass
