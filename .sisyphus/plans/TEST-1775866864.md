# TEST-1775866864 — TEST-1775866864

Add a `formatDateTime` function to complement the existing `formatDate` function in the codebase. The new function will extend date-only formatting capabilities to include time component support (hours, minutes, seconds), following the ISO 8601 datetime format standard.

## Wave 1

- [x] 1. Add formatDateTime function to src/index.ts
  - Function signature: `formatDateTime(date: Date): string`
  - Returns datetime in ISO 8601 format: "YYYY-MM-DDTHH:mm:ss"
  - Leverages date.toISOString() for reliable formatting
- [x] 2. Add unit tests for formatDateTime in src/index.test.ts
  - Test basic datetime formatting with known date and time
  - Test morning time (00:00:00)
  - Test evening time (23:59:59)
  - Test midday time (12:30:45)
- [x] 3. Run tests to verify implementation
  - Execute `npm test` using vitest
  - All tests must pass before proceeding
- [x] 4. Run lint/typecheck to ensure code quality
  - Execute `npm run lint` to run TypeScript type checking
  - Execute `npm run build` to verify compilation succeeds
  - Fix any type errors or warnings if discovered
