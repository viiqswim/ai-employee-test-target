# TEST-1775869708 — Add formatCurrency utility function

Implement a `formatCurrency(amount: number, currency?: string): string` utility function that formats numbers as currency strings with proper locale formatting, support for negative values, and multiple currency symbols.

## Wave 1

- [x] 1. Add unit tests for formatCurrency with default USD formatting (1234.5 → '$1,234.50')
- [x] 2. Add unit tests for large number formatting (1000000 → '$1,000,000.00')
- [x] 3. Add unit tests for negative number formatting (-99.99 → '-$99.99')
- [x] 4. Add unit tests for EUR currency formatting ('EUR' → '€' symbol prefix)
- [x] 5. Add unit tests for GBP currency formatting ('GBP' → '£' symbol prefix)
- [x] 6. Add unit tests for JPY currency formatting ('JPY' → '¥' symbol, no decimal places)

## Wave 2

- [ ] 1. Implement formatCurrency function with proper TypeScript signature and locale-aware number formatting
- [ ] 2. Add support for negative currency values with minus sign before currency symbol
- [ ] 3. Add currency symbol mapping for USD, EUR, GBP, and JPY currencies
- [ ] 4. Ensure JPY currency formats without decimal places per standard currency conventions

## Wave 3

- [ ] 1. Run TypeScript compiler (npm run lint) to verify no type errors
- [ ] 2. Run test suite (npm test) to verify all acceptance criteria pass
- [ ] 3. Run build (npm run build) to verify project compiles successfully