# TEST-1776106758 — Add formatCurrency utility function

Add a `formatCurrency(amount: number, currency?: string): string` function to `src/index.ts` that formats numbers as currency strings with proper locale-aware thousand separators and decimal handling. The implementation must support USD as the default currency and EUR as an alternative, with correct handling of negative values.

## Wave 1

- [x] 1. Implement formatCurrency function in src/index.ts
- [x] 2. Add Vitest unit tests for formatCurrency in src/index.test.ts
- [x] 3. Verify TypeScript compiles without errors using tsc

## Wave 2

- [x] 1. Run full test suite to verify all tests pass
