# TEST-1775855651 — Add formatCurrency utility function

Implement a `formatCurrency` utility function that formats numbers as currency strings with proper locale-aware thousand separators and decimal formatting, supporting USD (default) and other currencies.

## Wave 1

- [x] 1. Implement formatCurrency function in src/index.ts
- [x] 2. Add unit tests for formatCurrency covering all acceptance criteria in src/index.test.ts
- [x] 3. Run TypeScript compiler to verify no type errors
- [x] 4. Run Vitest tests to verify all tests pass