# TEST-1775842363 — Add formatCurrency utility function

This plan implements a `formatCurrency(amount: number, currency?: string): string` utility function in `src/index.ts` with comprehensive unit tests.

The implementation will handle USD (default) formatting with thousand separators and two decimal places, negative numbers, and optional currency codes (EUR).

## Wave 1

- [x] 1. Implement formatCurrency function in src/index.ts with USD default
- [x] 2. Add formatCurrency unit tests covering USD formatting
- [x] 3. Add tests for negative number formatting

## Wave 2

- [x] 1. Add support for EUR currency code parameter
- [x] 2. Add unit tests for EUR currency formatting
- [x] 3. Run TypeScript compiler and lint to verify clean build